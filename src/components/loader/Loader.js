import React from "react";
import "../../App.scss"
const Loader = () => {
    return (
        <div className="container">
            <div className="loader">Loading...</div>
        </div>
    );
};

export default Loader;